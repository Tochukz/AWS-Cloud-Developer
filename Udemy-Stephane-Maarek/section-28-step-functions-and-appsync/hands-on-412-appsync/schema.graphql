type Book {
  id: ID!
  title: String!
  author: String!
}

type Query {
  getBook(id: ID!): Book
  listBooks: [Book]
}

type Mutation {
  addBook(id: ID!, title: String!, author: String!): Book
}

schema {
  query: Query
  mutation: Mutation
}
